name: Check with SonarQube
on:
  workflow_dispatch:

permissions: 
  actions: read
  security-events: write

jobs:
  Check_with_SonarQube: 
    runs-on: ubuntu-22.04

    steps:
       
      - name: generate predefined macros include file
        run: |
          touch predefined_macros.c
          cat predefined_macros.h
